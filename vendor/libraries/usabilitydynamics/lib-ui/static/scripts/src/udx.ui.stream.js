/**
 * Steram
 *
 */
define( 'udx.ui.stream', [ 'udx.utility' ], function Swiper() {
  console.debug( 'udx.ui.stream', 'loaded' );

  return function domnReady() {
    console.debug( 'udx.ui.stream', 'ready' );

    return this;

  };

});

